# One compiler for every machine and for CI. New Rust releases add lints, and
# CI treats warnings as errors, so an unpinned toolchain breaks the build on
# its own. Bump this deliberately.
[toolchain]
channel = "1.93.0"
components = ["clippy", "rustfmt"]
